/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    nextConfig,
    images: {
        domains: ['fakestoreapi.com'], // Add the hostname(s) of your image source(s)
      },
}
